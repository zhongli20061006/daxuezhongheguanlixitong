# AGENTS.md

本文件是仓库内所有 AI 助手（主代理与子代理）的强制性行为守则。代理在动手前必须完整阅读并遵守；若与任务消息冲突，以任务消息的明确指令为准，但任务消息未明确授权的范围一律不碰。

## 1. 子代理铁律（重中之重）

1. 只做任务消息明确要求的那一件事。
2. 禁止主动读取任务未指定的文件或目录，包括且不限于：`docs/`、`.agents/`、`.codex/`、`.superpawers/`、计划与设计文档、其他任务的产出。
3. 禁止自行扩大范围：不得"顺手"实现相邻功能、不得继续执行计划里的后续步骤、不得修复任务外的 bug。
4. 完成后必须停下并回报结果，等待下一条指令；不得自行判断"下一步"并继续。
5. 未经任务消息明确授权，禁止：git 操作（add/commit/push）、运行测试、启动/停止服务、读写数据库、删除文件、安装依赖、访问网络。
6. 禁止派生子代理。
7. 任务描述有歧义时，停下来说明歧义并等待指令，不得猜测后扩大执行。

## 2. 仓库事实

- 项目：智伴校园（FastAPI + Vue 3 + MySQL，含本地 Ollama 智能体）。
- git 命令必须带 `-c safe.directory="D:/DjangoProject/学生管理系统（实验）"`。
- 禁止提交：`.omo/`、`.superpawers/`、`技术设计文档.docx`（`.env` 已被 gitignore）。
- 后端测试：`.venv\Scripts\python.exe -m pytest tests -q -p no:cacheprovider`（当前基准 173/173 通过）。
- 前端构建：在 `student-management-frontend/` 下执行 `npm run build`。
- 推送网络受限："Connection was reset" 但输出含 ref 更新行 = 实际成功，用 `git status -sb` 核实。
- 中文输出在 PowerShell 控制台显示乱码是编码问题；写脚本文件用 UTF-8。

## 3. 主代理派发约定

- 派发任务必须自包含：目标、允许做的事、禁止做的事、"完成后回报并停止"。
- 涉及实现计划时，把该任务所需的全部信息写进消息，不要让子代理去读计划文件。
- 子代理回报后由主代理审查并决定下一步，子代理不得自行推进。
